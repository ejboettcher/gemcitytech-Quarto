% fancy header
\usepackage{geometry}
\usepackage{fancyhdr}
\fancypagestyle{custom-fancy}{%
    \newgeometry{
        top=25mm,
        left=30mm,
        right=30mm,
        bottom=25mm,
        includeheadfoot
    }
    
$if(classification)$
$if(classification_color)$
    \fancyhead[C]{\textcolor{$classification_color$}{\textbf{$classification$}}}
    \fancyfoot[C]{\textcolor{$classification_color$}{\textbf{$classification$}}}
$else$
    \fancyfoot[C]{$classification$}
$endif$
$endif$
\fancyfoot[R]{\thepage}
}
